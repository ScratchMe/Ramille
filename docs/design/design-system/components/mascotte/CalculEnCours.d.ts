/** Écran d’attente du calcul — mascotte thinking 72, sans barre ni décompte. */
export interface CalculEnCoursProps {
  style?: React.CSSProperties;
}
export declare function CalculEnCours(props: CalculEnCoursProps): JSX.Element;
