/** Écran d’ouverture — continuation du splash, la feuille se pose puis sourit. */
export interface EcranLancementProps {
  mood?: 'calm' | 'happy';
  style?: React.CSSProperties;
}
export declare function EcranLancement(props: EcranLancementProps): JSX.Element;
