Les indisponibilités disent pourquoi (dans `detail`), et sur web la ligne notification n’existe pas plutôt que d’être grisée.

```jsx
<ChoixDeRappel canal="push" />
```

Même rangée que la feuille des rappels : ChoiceRow avec detail.
