/** Enveloppe le titre d'un écran qui en remplace un autre sous le doigt, et y porte le focus au montage. */
export interface TitreDArriveeProps {
  style?: React.CSSProperties;
  children?: React.ReactNode;
}
export declare function TitreDArrivee(props: TitreDArriveeProps): JSX.Element;
