S’ouvre une fois par appareil, juste après l’engagement. Ramille peut parler ici : aucun chiffre sur la feuille.

```jsx
<FeuilleRappels boucle="hebdo" canal="push" />
```

Posée en bas d’un scrim `--color-scrim`. Le bouton n’annonce un dialogue système que s’il va vraiment s’en ouvrir un.
