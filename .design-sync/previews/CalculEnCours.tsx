import React from 'react';
import { CalculEnCours } from 'ramille-design-system';

/**
 * L'attente du calcul : expression `thinking`, et surtout ni barre de progression ni
 * décompte — on ne promet pas une durée qu'on ne connaît pas.
 */
export const Attente = () => <CalculEnCours />;
